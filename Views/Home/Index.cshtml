@{
    ViewData["Title"] = "Page d'accueil";
}

<div class="container mt-5">
    <div class="text-center mb-5">
        <h1 class="display-4 fw-bold text-primary">👋 Bienvenue sur <span class="text-dark">UserRoles</span></h1>
        <p class="lead text-muted">Créez votre profil, téléchargez votre CV, et accédez facilement à des opportunités professionnelles.</p>
    </div>

    <!-- Section conditionnelle : Connexion ou boutons -->
    <div class="row justify-content-center">
        <div class="col-md-10 col-lg-8">
            @if (User.Identity.IsAuthenticated)
            {
                <div class="alert alert-success p-4 mb-5 rounded-4 shadow-sm text-center">
                    <h5 class="mb-3">✅ Connexion réussie !</h5>
                    <p class="fs-5 mb-0">
                        <i class="bi bi-person-circle text-primary me-2 fs-4"></i>
                        Bienvenue <strong>@ViewBag.FullName</strong> sur <strong>UserRoles</strong> 🎉
                    </p>
                </div>
            }
            else
            {
                <div class="d-flex justify-content-center gap-4 flex-wrap mb-5">
                    <a class="btn btn-success btn-lg px-4" asp-controller="Account" asp-action="Login">
                        <i class="bi bi-box-arrow-in-right me-1"></i> Se connecter
                    </a>
                    <a class="btn btn-outline-info btn-lg px-4" asp-controller="Account" asp-action="Register">
                        <i class="bi bi-person-plus me-1"></i> Créer un compte
                    </a>
                </div>
            }

            <div class="card shadow-lg border-0 bg-light mb-5 rounded-4">
                <div class="card-body p-5">
                    <h4 class="card-title text-success mb-4">
                        <i class="bi bi-check2-circle me-2"></i>Pourquoi se connecter ?
                    </h4>
                    <ul class="list-unstyled fs-5 mb-4">
                        <li class="mb-3">
                            <i class="bi bi-file-earmark-person-fill text-primary me-2 fs-4"></i>
                            Ajoutez votre CV pour le rendre visible aux recruteurs.
                        </li>
                        <li class="mb-3">
                            <i class="bi bi-tools text-warning me-2 fs-4"></i>
                            Mettez à jour votre profil avec vos compétences.
                        </li>
                        <li class="mb-3">
                            <i class="bi bi-briefcase-fill text-info me-2 fs-4"></i>
                            Suivez vos candidatures et gérez vos offres.
                        </li>
                    </ul>
                    <div class="alert alert-info rounded-3">
                        Rejoignez-nous dès maintenant pour profiter de toutes les fonctionnalités de la plateforme.
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
